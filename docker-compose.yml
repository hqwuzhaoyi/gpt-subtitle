version: "3"

services:
  api:
    container_name: api
    build:
      context: .
      dockerfile: ./apps/server/Dockerfile
    restart: always
    volumes:
      - /your_whisper_dir/whisper:/app/whisper
      - /your_uploads_dir/uploads:/app/uploads
    ports:
      - 3001:3001
    networks:
      - app_network
    env_file:
      - .env
  web:
    container_name: web
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
    restart: always
    ports:
      - 3000:3000
    networks:
      - app_network
    env_file:
      - .env
    depends_on:
      - api

# Define a network, which allows containers to communicate
# with each other, by using their container name as a hostname
networks:
  app_network:
    external: true
